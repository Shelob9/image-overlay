/** @jsx jsx */
import { jsx } from "theme-ui";

import * as React from "react";
import { Styled } from "theme-ui";

export const PageTitle = ({ children }) => <Styled.h1>{children}</Styled.h1>;
